<%= form_with(model: client_user, url: client_user_path(client_user), local: true) do |form| %>
  <% if client_user.errors.any? %>
    <div id="error_explanation" class="alert alert-danger" role="alert">
      <h5><%= pluralize(client_user.errors.count, "error") %> prohibited this client_user from being saved:</h5>
      <ul>
      <% client_user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
       <div class="form-group">
        <%= form.label :nome %>
        <%= form.text_field :nome, id: :nome ,class:"form-control"%>
      </div>
      <div class="form-group">
        <%= form.label :telefone %>
        <%= form.text_field :telefone, id: :telefone ,class:"form-control"%>
      </div>
        <div class="form-group">
        <%= form.label :cpf %>
        <%= form.text_field :cpf, id: :cpf ,class:"form-control"%>
      </div>
       <div class="form-group">
        <%= form.label :email %>
        <%= form.text_field :email, id: :email ,class:"form-control"%>
      </div>
        <div class="actions">
    <%= form.submit "Salvar", class:"btn btn-success min-width-btn mt-2" %>
  </div>
<% end %>